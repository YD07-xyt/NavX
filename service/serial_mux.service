[Unit]
After=graphical-session.target

[Service]
ExecStart=%h/serial_mux/target/release/serial_mux --config %h/serial_mux/config.toml
Environment=RUST_LOG=info
Restart=on-failure
RestartSec=5

[Install]
WantedBy=graphical-session.target
